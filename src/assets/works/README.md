# 作品与模型目录

将作品图片或模型放入此目录。图片支持 `.png`、`.jpg`、`.jpeg`、`.webp`、`.gif`、`.avif`；模型支持 `.glb`、`.gltf`、`.fbx` 和 `.obj`。项目作品墙会自动读取文件，并根据更新时间将最新作品排在最前。文件名会作为临时作品标题。OBJ 的外部材质与贴图建议后续转换为 FBX 或贴图内嵌的 GLB，以保证发布后的材质路径稳定。
